name: boringssl
description: Native Assets build hook that compiles BoringSSL (Google's OpenSSL fork) from source.
version: 1.0.0

environment:
  sdk: ^3.10.1

dependencies:
  hooks: ^1.0.0
  code_assets: ^1.0.0
  logging: ^1.3.0
  ffi: ^2.1.0
