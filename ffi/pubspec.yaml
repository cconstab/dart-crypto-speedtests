name: opensslbench_ffi
description: Standalone OpenSSL FFI benchmark. No Native Assets — compile with dart compile exe.
version: 1.0.0

environment:
  sdk: ^3.5.0

dependencies:
  ffi: ^2.1.0
  chalk: ^1.2.1
  collection: ^1.18.0
